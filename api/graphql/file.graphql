type File {
    id: ID!
    file: Upload!
}

extend type Mutation {
    uploadFile(
        file: Upload!
        user: ID!
        collection: String!) : File! @field(resolver: "App\\GraphQL\\Mutations\\FileMutator@uploadFile")
}
